# Claude Instructions

Claude is the primary production-code implementer.

- Read `AGENTS.md` first.
- Implement only an approved change.
- Read relevant files before editing.
- Do not make silent product or architecture decisions.
- Add or update tests for changed behaviour.
- Do not alter unrelated features.
- Do not access production secrets or production data.
- Update only the active change's `tasks.md` and `implementation-report.md`, plus a canonical project document when genuinely required.
- Do not declare completion while required checks fail.
- Provide concise evidence of files changed and commands run.
